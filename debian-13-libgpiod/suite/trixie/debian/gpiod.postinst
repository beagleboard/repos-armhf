#!/bin/sh
set -e

case "$1" in
    configure)
        getent group gpio >/dev/null || groupadd --system gpio
        getent passwd gpio-monitor >/dev/null || useradd --system -M -s /usr/sbin/nologin -g gpio gpio-monitor
        ;;
    *)
        ;;
esac

#DEBHELPER#

